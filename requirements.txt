fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic==2.5.0
python-multipart==0.0.6
pytest==7.4.3
pytest-asyncio==0.21.1
passlib[bcrypt]>=1.7.4
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
# Oracle database driver
oracledb>=2.0.0
# Monitoring dependencies (optional)
prometheus-client>=0.19.0  # For Prometheus metrics
# LLM dependencies - Ollama via LangChain (FREE, local, no API key required)
langchain>=0.1.0  # LangChain framework
langchain-ollama>=0.1.0  # LangChain Ollama integration (uses ChatOllama with invoke method)

# TTS dependencies (optional - install based on provider choice)
piper-tts>=1.2.0  # For Piper TTS (recommended - lightweight, fast)
# TTS>=0.20.0  # For Coqui TTS (alternative - uncomment if using Coqui)
# soundfile>=0.12.0  # Required for Coqui TTS (uncomment if using Coqui)

# Redis for caching
redis>=5.0.0  # Redis client for Python
# Vector database for RAG
chromadb>=0.4.0
sentence-transformers>=2.2.0
