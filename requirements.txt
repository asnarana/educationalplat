fastapi==0.104.1
uvicorn[standard]==0.24.0
sqlalchemy==2.0.23
pydantic==2.5.0
python-multipart==0.0.6
pytest==7.4.3
pytest-asyncio==0.21.1
# LLM dependencies (optional - install based on provider choice)
# FREE: Ollama (local, no API key required)
langchain>=0.1.0  # Langchain framework
langchain-ollama>=0.1.0  # Langchain Ollama integration (FREE - newer recommended package)
# PAID: Optional providers (uncomment if needed)
# openai>=1.0.0  # For OpenAI (requires API key, costs money)
# huggingface-hub>=0.19.0  # For HuggingFace (requires API key, may cost money)

# TTS dependencies (optional - install based on provider choice)
piper-tts>=1.2.0  # For Piper TTS (recommended - lightweight, fast)
# TTS>=0.20.0  # For Coqui TTS (alternative - uncomment if using Coqui)
# soundfile>=0.12.0  # Required for Coqui TTS (uncomment if using Coqui)

